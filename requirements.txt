# Soundscape Audio Analysis - dipendenze Python 3.12
# Pin stringenti per riproducibilità

# Numerico e I/O audio
# numpy pinnato <2.0 per compatibilita' con laion-clap 1.1.7 e tensorflow 2.x
# (v0.9.0: pin stretto dopo incidente scikit-maad 1.5.2 che tirava numpy 2.2.6
# via scikit-image 0.26 / tifffile 2026, rompendo laion-clap + panns_inference).
numpy>=1.26,<2.0
scipy>=1.11,<1.15
librosa>=0.10,<0.11
soundfile>=0.12,<0.13
resampy>=0.4.3

# Grafica
matplotlib>=3.8,<3.10

# Machine learning audio
tensorflow>=2.16,<2.20
tensorflow-hub>=0.16

# PANNs classifier (v0.2.0)
torch>=2.2
torchaudio>=2.2
panns_inference>=0.1.1

# CLAP auto-tagging (v0.2.1)
laion_clap>=1.1.6
transformers>=4.40

# Speech transcription (v0.5.0)
# faster-whisper usa CTranslate2 (CPU + int8 su Apple Silicon via NEON SIMD)
faster-whisper>=1.0,<2
silero-vad>=5.0,<6


# PDF
reportlab>=4.0,<4.4

# CLI
click>=8.1,<8.3
rich>=13.7

# Ecoacoustic indices (v0.9.0 Step A):
# scikit-maad fornisce le implementazioni peer-reviewed di ACI/NDSI/H/BI/ADI/AEI
# (Ulloa et al. 2021, SoftwareX, DOI 10.1016/j.softx.2021.100720).
# Pin stretto su 1.4.3 + scikit-image<0.23 + tifffile<2024 per evitare la
# catena transitiva che esigerebbe numpy>=2 e romperebbe laion-clap/tensorflow.
# Il wrapper scripts/ecoacoustic_maad.py e' opt-in via --ecoacoustic-backend maad.
scikit-maad==1.4.3
scikit-image<0.23
tifffile<2024
pywavelets>=1.4,<1.6

# Testing e validazione
pytest>=8.0
jsonschema>=4.20

# pkg_resources deprecato da setuptools 82, tensorflow-hub lo richiede ancora
setuptools<81
